import { PlaylistProfileImagePipe } from './playlist-profile-image.pipe';

describe('PlaylistProfileImagePipe', () => {
  it('create an instance', () => {
    const pipe = new PlaylistProfileImagePipe();
    expect(pipe).toBeTruthy();
  });
});
